import { createSlice } from '@reduxjs/toolkit';

const CreatePortalSlice = createSlice({
  name: 'CreatePortal',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default CreatePortalSlice.reducer;
export const {} = CreatePortalSlice.actions;
