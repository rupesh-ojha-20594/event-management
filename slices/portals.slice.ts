import { createSlice } from '@reduxjs/toolkit';

const PortalsSlice = createSlice({
  name: 'Portals',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default PortalsSlice.reducer;
export const {} = PortalsSlice.actions;
