import { createSlice } from '@reduxjs/toolkit';

const WorkflowRulesEditSlice = createSlice({
  name: 'WorkflowRulesEdit',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default WorkflowRulesEditSlice.reducer;
export const {} = WorkflowRulesEditSlice.actions;
