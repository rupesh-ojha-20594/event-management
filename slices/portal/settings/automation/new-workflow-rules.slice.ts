import { createSlice } from '@reduxjs/toolkit';

const NewWorkflowRulesSlice = createSlice({
  name: 'NewWorkflowRules',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default NewWorkflowRulesSlice.reducer;
export const {} = NewWorkflowRulesSlice.actions;
