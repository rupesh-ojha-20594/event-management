import { createSlice } from '@reduxjs/toolkit';

const CustomFunctionsEditSlice = createSlice({
  name: 'CustomFunctionsEdit',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default CustomFunctionsEditSlice.reducer;
export const {} = CustomFunctionsEditSlice.actions;
