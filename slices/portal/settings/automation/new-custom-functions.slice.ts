import { createSlice } from '@reduxjs/toolkit';

const NewCustomFunctionsSlice = createSlice({
  name: 'NewCustomFunctions',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default NewCustomFunctionsSlice.reducer;
export const {} = NewCustomFunctionsSlice.actions;
