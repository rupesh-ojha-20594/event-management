import { createSlice } from '@reduxjs/toolkit';

const WorkflowRulesSlice = createSlice({
  name: 'WorkflowRules',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default WorkflowRulesSlice.reducer;
export const {} = WorkflowRulesSlice.actions;
