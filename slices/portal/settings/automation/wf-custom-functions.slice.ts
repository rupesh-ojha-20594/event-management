import { createSlice } from '@reduxjs/toolkit';

const WfCustomFunctionsSlice = createSlice({
  name: 'WfCustomFunctions',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default WfCustomFunctionsSlice.reducer;
export const {} = WfCustomFunctionsSlice.actions;
