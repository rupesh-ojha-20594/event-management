import { createSlice } from '@reduxjs/toolkit';

const TaxAuthoritySlice = createSlice({
  name: 'TaxAuthority',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default TaxAuthoritySlice.reducer;
export const {} = TaxAuthoritySlice.actions;
