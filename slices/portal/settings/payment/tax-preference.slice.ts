import { createSlice } from '@reduxjs/toolkit';

const TaxPreferenceSlice = createSlice({
  name: 'TaxPreference',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default TaxPreferenceSlice.reducer;
export const {} = TaxPreferenceSlice.actions;
