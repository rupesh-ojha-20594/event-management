import { createSlice } from '@reduxjs/toolkit';

const WebConferenceSlice = createSlice({
  name: 'WebConference',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default WebConferenceSlice.reducer;
export const {} = WebConferenceSlice.actions;
