import { createSlice } from '@reduxjs/toolkit';

const BsCrmSlice = createSlice({
  name: 'BsCrm',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default BsCrmSlice.reducer;
export const {} = BsCrmSlice.actions;
