import { createSlice } from '@reduxjs/toolkit';

const TriggersSlice = createSlice({
  name: 'Triggers',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default TriggersSlice.reducer;
export const {} = TriggersSlice.actions;
