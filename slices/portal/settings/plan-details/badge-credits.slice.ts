import { createSlice } from '@reduxjs/toolkit';

const BadgeCreditsSlice = createSlice({
  name: 'BadgeCredits',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default BadgeCreditsSlice.reducer;
export const {} = BadgeCreditsSlice.actions;
