import { createSlice } from '@reduxjs/toolkit';

const BoothCreditsSlice = createSlice({
  name: 'BoothCredits',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default BoothCreditsSlice.reducer;
export const {} = BoothCreditsSlice.actions;
