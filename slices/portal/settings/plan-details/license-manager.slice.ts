import { createSlice } from '@reduxjs/toolkit';

const LicenseManagerSlice = createSlice({
  name: 'LicenseManager',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default LicenseManagerSlice.reducer;
export const {} = LicenseManagerSlice.actions;
