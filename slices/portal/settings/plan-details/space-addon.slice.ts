import { createSlice } from '@reduxjs/toolkit';

const SpaceAddonSlice = createSlice({
  name: 'SpaceAddon',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default SpaceAddonSlice.reducer;
export const {} = SpaceAddonSlice.actions;
