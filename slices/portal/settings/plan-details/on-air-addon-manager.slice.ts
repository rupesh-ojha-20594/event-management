import { createSlice } from '@reduxjs/toolkit';

const OnAirAddonManagerSlice = createSlice({
  name: 'OnAirAddonManager',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default OnAirAddonManagerSlice.reducer;
export const {} = OnAirAddonManagerSlice.actions;
