import { createSlice } from '@reduxjs/toolkit';

const AttendeeCreditsSlice = createSlice({
  name: 'AttendeeCredits',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default AttendeeCreditsSlice.reducer;
export const {} = AttendeeCreditsSlice.actions;
