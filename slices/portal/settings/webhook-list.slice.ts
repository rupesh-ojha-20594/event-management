import { createSlice } from '@reduxjs/toolkit';

const WebhookListSlice = createSlice({
  name: 'WebhookList',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default WebhookListSlice.reducer;
export const {} = WebhookListSlice.actions;
