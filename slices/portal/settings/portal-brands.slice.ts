import { createSlice } from '@reduxjs/toolkit';

const PortalBrandsSlice = createSlice({
  name: 'PortalBrands',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default PortalBrandsSlice.reducer;
export const {} = PortalBrandsSlice.actions;
