import { createSlice } from '@reduxjs/toolkit';

const RolesAndPrivilegesSlice = createSlice({
  name: 'RolesAndPrivileges',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default RolesAndPrivilegesSlice.reducer;
export const {} = RolesAndPrivilegesSlice.actions;
