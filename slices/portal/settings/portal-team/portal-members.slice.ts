import { createSlice } from '@reduxjs/toolkit';

const PortalMembersSlice = createSlice({
  name: 'PortalMembers',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default PortalMembersSlice.reducer;
export const {} = PortalMembersSlice.actions;
