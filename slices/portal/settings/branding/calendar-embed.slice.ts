import { createSlice } from '@reduxjs/toolkit';

const CalendarEmbedSlice = createSlice({
  name: 'CalendarEmbed',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default CalendarEmbedSlice.reducer;
export const {} = CalendarEmbedSlice.actions;
