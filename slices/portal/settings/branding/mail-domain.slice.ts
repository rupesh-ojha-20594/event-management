import { createSlice } from '@reduxjs/toolkit';

const MailDomainSlice = createSlice({
  name: 'MailDomain',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default MailDomainSlice.reducer;
export const {} = MailDomainSlice.actions;
