import { createSlice } from '@reduxjs/toolkit';

const SubDomainSlice = createSlice({
  name: 'SubDomain',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default SubDomainSlice.reducer;
export const {} = SubDomainSlice.actions;
