import { createSlice } from '@reduxjs/toolkit';

const AppRequestSlice = createSlice({
  name: 'AppRequest',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default AppRequestSlice.reducer;
export const {} = AppRequestSlice.actions;
