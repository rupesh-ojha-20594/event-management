import { createSlice } from '@reduxjs/toolkit';

const CustomDomainSlice = createSlice({
  name: 'CustomDomain',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default CustomDomainSlice.reducer;
export const {} = CustomDomainSlice.actions;
