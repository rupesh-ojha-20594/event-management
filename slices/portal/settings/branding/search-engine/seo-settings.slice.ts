import { createSlice } from '@reduxjs/toolkit';

const SeoSettingsSlice = createSlice({
  name: 'SeoSettings',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default SeoSettingsSlice.reducer;
export const {} = SeoSettingsSlice.actions;
