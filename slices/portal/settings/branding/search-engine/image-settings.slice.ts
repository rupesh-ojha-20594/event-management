import { createSlice } from '@reduxjs/toolkit';

const ImageSettingsSlice = createSlice({
  name: 'ImageSettings',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default ImageSettingsSlice.reducer;
export const {} = ImageSettingsSlice.actions;
