import { createSlice } from '@reduxjs/toolkit';

const BrandCustomJsSlice = createSlice({
  name: 'BrandCustomJs',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default BrandCustomJsSlice.reducer;
export const {} = BrandCustomJsSlice.actions;
