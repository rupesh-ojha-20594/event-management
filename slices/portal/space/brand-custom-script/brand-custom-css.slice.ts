import { createSlice } from '@reduxjs/toolkit';

const BrandCustomCssSlice = createSlice({
  name: 'BrandCustomCss',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default BrandCustomCssSlice.reducer;
export const {} = BrandCustomCssSlice.actions;
