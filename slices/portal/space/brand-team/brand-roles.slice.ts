import { createSlice } from '@reduxjs/toolkit';

const BrandRolesSlice = createSlice({
  name: 'BrandRoles',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default BrandRolesSlice.reducer;
export const {} = BrandRolesSlice.actions;
