import { createSlice } from '@reduxjs/toolkit';

const BrandMembersSlice = createSlice({
  name: 'BrandMembers',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default BrandMembersSlice.reducer;
export const {} = BrandMembersSlice.actions;
