import { createSlice } from '@reduxjs/toolkit';

const FormResponseSlice = createSlice({
  name: 'FormResponse',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default FormResponseSlice.reducer;
export const {} = FormResponseSlice.actions;
