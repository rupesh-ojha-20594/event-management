import { createSlice } from '@reduxjs/toolkit';

const VcVisibilitySlice = createSlice({
  name: 'VcVisibility',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default VcVisibilitySlice.reducer;
export const {} = VcVisibilitySlice.actions;
