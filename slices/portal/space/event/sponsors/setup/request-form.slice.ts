import { createSlice } from '@reduxjs/toolkit';

const RequestFormSlice = createSlice({
  name: 'RequestForm',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default RequestFormSlice.reducer;
export const {} = RequestFormSlice.actions;
