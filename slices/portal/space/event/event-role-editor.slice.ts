import { createSlice } from '@reduxjs/toolkit';

const EventRoleEditorSlice = createSlice({
  name: 'EventRoleEditor',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default EventRoleEditorSlice.reducer;
export const {} = EventRoleEditorSlice.actions;
