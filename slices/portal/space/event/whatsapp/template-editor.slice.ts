import { createSlice } from '@reduxjs/toolkit';

const TemplateEditorSlice = createSlice({
  name: 'TemplateEditor',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default TemplateEditorSlice.reducer;
export const {} = TemplateEditorSlice.actions;
