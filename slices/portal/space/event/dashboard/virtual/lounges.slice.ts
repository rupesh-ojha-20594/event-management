import { createSlice } from '@reduxjs/toolkit';

const LoungesSlice = createSlice({
  name: 'Lounges',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default LoungesSlice.reducer;
export const {} = LoungesSlice.actions;
