import { createSlice } from '@reduxjs/toolkit';

const WebcastStreamsSlice = createSlice({
  name: 'WebcastStreams',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default WebcastStreamsSlice.reducer;
export const {} = WebcastStreamsSlice.actions;
