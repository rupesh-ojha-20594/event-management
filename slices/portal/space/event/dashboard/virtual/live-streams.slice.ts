import { createSlice } from '@reduxjs/toolkit';

const LiveStreamsSlice = createSlice({
  name: 'LiveStreams',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default LiveStreamsSlice.reducer;
export const {} = LiveStreamsSlice.actions;
