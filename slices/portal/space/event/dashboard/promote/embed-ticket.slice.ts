import { createSlice } from '@reduxjs/toolkit';

const EmbedTicketSlice = createSlice({
  name: 'EmbedTicket',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default EmbedTicketSlice.reducer;
export const {} = EmbedTicketSlice.actions;
