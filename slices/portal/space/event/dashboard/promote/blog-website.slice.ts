import { createSlice } from '@reduxjs/toolkit';

const BlogWebsiteSlice = createSlice({
  name: 'BlogWebsite',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default BlogWebsiteSlice.reducer;
export const {} = BlogWebsiteSlice.actions;
