import { createSlice } from '@reduxjs/toolkit';

const SitePreferencesSlice = createSlice({
  name: 'SitePreferences',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default SitePreferencesSlice.reducer;
export const {} = SitePreferencesSlice.actions;
