import { createSlice } from '@reduxjs/toolkit';

const UsersLeaderboardSlice = createSlice({
  name: 'UsersLeaderboard',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default UsersLeaderboardSlice.reducer;
export const {} = UsersLeaderboardSlice.actions;
