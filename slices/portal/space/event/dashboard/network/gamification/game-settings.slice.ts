import { createSlice } from '@reduxjs/toolkit';

const GameSettingsSlice = createSlice({
  name: 'GameSettings',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default GameSettingsSlice.reducer;
export const {} = GameSettingsSlice.actions;
