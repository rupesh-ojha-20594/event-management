import { createSlice } from '@reduxjs/toolkit';

const VideoLibrarySlice = createSlice({
  name: 'VideoLibrary',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default VideoLibrarySlice.reducer;
export const {} = VideoLibrarySlice.actions;
