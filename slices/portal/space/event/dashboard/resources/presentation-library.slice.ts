import { createSlice } from '@reduxjs/toolkit';

const PresentationLibrarySlice = createSlice({
  name: 'PresentationLibrary',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default PresentationLibrarySlice.reducer;
export const {} = PresentationLibrarySlice.actions;
