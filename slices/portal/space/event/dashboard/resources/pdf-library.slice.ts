import { createSlice } from '@reduxjs/toolkit';

const PdfLibrarySlice = createSlice({
  name: 'PdfLibrary',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default PdfLibrarySlice.reducer;
export const {} = PdfLibrarySlice.actions;
