import { createSlice } from '@reduxjs/toolkit';

const EventGallerySlice = createSlice({
  name: 'EventGallery',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default EventGallerySlice.reducer;
export const {} = EventGallerySlice.actions;
