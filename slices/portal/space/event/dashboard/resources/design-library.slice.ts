import { createSlice } from '@reduxjs/toolkit';

const DesignLibrarySlice = createSlice({
  name: 'DesignLibrary',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default DesignLibrarySlice.reducer;
export const {} = DesignLibrarySlice.actions;
