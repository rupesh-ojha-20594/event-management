import { createSlice } from '@reduxjs/toolkit';

const BatchCodesSlice = createSlice({
  name: 'BatchCodes',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default BatchCodesSlice.reducer;
export const {} = BatchCodesSlice.actions;
