import { createSlice } from '@reduxjs/toolkit';

const DiscountRuleSlice = createSlice({
  name: 'DiscountRule',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default DiscountRuleSlice.reducer;
export const {} = DiscountRuleSlice.actions;
