import { createSlice } from '@reduxjs/toolkit';

const AccessCodeSlice = createSlice({
  name: 'AccessCode',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default AccessCodeSlice.reducer;
export const {} = AccessCodeSlice.actions;
