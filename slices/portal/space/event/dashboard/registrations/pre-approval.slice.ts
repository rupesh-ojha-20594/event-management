import { createSlice } from '@reduxjs/toolkit';

const PreApprovalSlice = createSlice({
  name: 'PreApproval',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default PreApprovalSlice.reducer;
export const {} = PreApprovalSlice.actions;
