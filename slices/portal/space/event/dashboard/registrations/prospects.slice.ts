import { createSlice } from '@reduxjs/toolkit';

const ProspectsSlice = createSlice({
  name: 'Prospects',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default ProspectsSlice.reducer;
export const {} = ProspectsSlice.actions;
