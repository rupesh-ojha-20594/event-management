import { createSlice } from '@reduxjs/toolkit';

const AttendeesSlice = createSlice({
  name: 'Attendees',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default AttendeesSlice.reducer;
export const {} = AttendeesSlice.actions;
