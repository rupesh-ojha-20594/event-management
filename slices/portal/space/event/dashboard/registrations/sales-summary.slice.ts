import { createSlice } from '@reduxjs/toolkit';

const SalesSummarySlice = createSlice({
  name: 'SalesSummary',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default SalesSummarySlice.reducer;
export const {} = SalesSummarySlice.actions;
