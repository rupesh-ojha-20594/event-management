import { createSlice } from '@reduxjs/toolkit';

const MissedRegistrationSlice = createSlice({
  name: 'MissedRegistration',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default MissedRegistrationSlice.reducer;
export const {} = MissedRegistrationSlice.actions;
