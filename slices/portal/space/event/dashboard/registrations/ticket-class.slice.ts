import { createSlice } from '@reduxjs/toolkit';

const TicketClassSlice = createSlice({
  name: 'TicketClass',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default TicketClassSlice.reducer;
export const {} = TicketClassSlice.actions;
