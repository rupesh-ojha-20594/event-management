import { createSlice } from '@reduxjs/toolkit';

const OrderSummarySlice = createSlice({
  name: 'OrderSummary',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default OrderSummarySlice.reducer;
export const {} = OrderSummarySlice.actions;
