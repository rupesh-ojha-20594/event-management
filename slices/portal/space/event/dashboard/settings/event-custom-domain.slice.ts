import { createSlice } from '@reduxjs/toolkit';

const EventCustomDomainSlice = createSlice({
  name: 'EventCustomDomain',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default EventCustomDomainSlice.reducer;
export const {} = EventCustomDomainSlice.actions;
