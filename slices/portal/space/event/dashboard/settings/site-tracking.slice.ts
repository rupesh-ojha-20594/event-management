import { createSlice } from '@reduxjs/toolkit';

const SiteTrackingSlice = createSlice({
  name: 'SiteTracking',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default SiteTrackingSlice.reducer;
export const {} = SiteTrackingSlice.actions;
