import { createSlice } from '@reduxjs/toolkit';

const UserTrackingSlice = createSlice({
  name: 'UserTracking',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default UserTrackingSlice.reducer;
export const {} = UserTrackingSlice.actions;
