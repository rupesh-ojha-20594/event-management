import { createSlice } from '@reduxjs/toolkit';

const SignInSettingsSlice = createSlice({
  name: 'SignInSettings',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default SignInSettingsSlice.reducer;
export const {} = SignInSettingsSlice.actions;
