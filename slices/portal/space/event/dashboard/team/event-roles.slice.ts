import { createSlice } from '@reduxjs/toolkit';

const EventRolesSlice = createSlice({
  name: 'EventRoles',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default EventRolesSlice.reducer;
export const {} = EventRolesSlice.actions;
