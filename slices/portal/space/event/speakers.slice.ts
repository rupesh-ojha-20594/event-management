import { createSlice } from '@reduxjs/toolkit';

const SpeakersSlice = createSlice({
  name: 'Speakers',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default SpeakersSlice.reducer;
export const {} = SpeakersSlice.actions;
