import { createSlice } from '@reduxjs/toolkit';

const CompletenessSlice = createSlice({
  name: 'Completeness',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default CompletenessSlice.reducer;
export const {} = CompletenessSlice.actions;
