import { createSlice } from '@reduxjs/toolkit';

const SessionRatingSlice = createSlice({
  name: 'SessionRating',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default SessionRatingSlice.reducer;
export const {} = SessionRatingSlice.actions;
