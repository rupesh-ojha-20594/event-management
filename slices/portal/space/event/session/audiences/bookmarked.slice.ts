import { createSlice } from '@reduxjs/toolkit';

const BookmarkedSlice = createSlice({
  name: 'Bookmarked',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default BookmarkedSlice.reducer;
export const {} = BookmarkedSlice.actions;
