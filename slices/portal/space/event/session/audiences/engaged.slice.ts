import { createSlice } from '@reduxjs/toolkit';

const EngagedSlice = createSlice({
  name: 'Engaged',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default EngagedSlice.reducer;
export const {} = EngagedSlice.actions;
