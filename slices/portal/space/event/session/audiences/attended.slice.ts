import { createSlice } from '@reduxjs/toolkit';

const AttendedSlice = createSlice({
  name: 'Attended',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default AttendedSlice.reducer;
export const {} = AttendedSlice.actions;
