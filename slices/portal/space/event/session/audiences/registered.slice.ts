import { createSlice } from '@reduxjs/toolkit';

const RegisteredSlice = createSlice({
  name: 'Registered',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default RegisteredSlice.reducer;
export const {} = RegisteredSlice.actions;
