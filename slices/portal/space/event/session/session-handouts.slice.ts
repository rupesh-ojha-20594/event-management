import { createSlice } from '@reduxjs/toolkit';

const SessionHandoutsSlice = createSlice({
  name: 'SessionHandouts',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default SessionHandoutsSlice.reducer;
export const {} = SessionHandoutsSlice.actions;
