import { createSlice } from '@reduxjs/toolkit';

const PublishSlice = createSlice({
  name: 'Publish',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default PublishSlice.reducer;
export const {} = PublishSlice.actions;
