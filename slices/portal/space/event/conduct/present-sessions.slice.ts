import { createSlice } from '@reduxjs/toolkit';

const PresentSessionsSlice = createSlice({
  name: 'PresentSessions',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default PresentSessionsSlice.reducer;
export const {} = PresentSessionsSlice.actions;
