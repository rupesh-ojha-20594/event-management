import { createSlice } from '@reduxjs/toolkit';

const TestAttendeesSlice = createSlice({
  name: 'TestAttendees',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default TestAttendeesSlice.reducer;
export const {} = TestAttendeesSlice.actions;
