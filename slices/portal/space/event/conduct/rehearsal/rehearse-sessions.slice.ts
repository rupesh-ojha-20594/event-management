import { createSlice } from '@reduxjs/toolkit';

const RehearseSessionsSlice = createSlice({
  name: 'RehearseSessions',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default RehearseSessionsSlice.reducer;
export const {} = RehearseSessionsSlice.actions;
