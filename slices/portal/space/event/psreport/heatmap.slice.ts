import { createSlice } from '@reduxjs/toolkit';

const HeatmapSlice = createSlice({
  name: 'Heatmap',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default HeatmapSlice.reducer;
export const {} = HeatmapSlice.actions;
