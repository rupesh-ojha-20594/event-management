import { createSlice } from '@reduxjs/toolkit';

const MicrositeDashboardSlice = createSlice({
  name: 'MicrositeDashboard',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default MicrositeDashboardSlice.reducer;
export const {} = MicrositeDashboardSlice.actions;
