import { createSlice } from '@reduxjs/toolkit';

const CustomFormBuilderSlice = createSlice({
  name: 'CustomFormBuilder',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default CustomFormBuilderSlice.reducer;
export const {} = CustomFormBuilderSlice.actions;
