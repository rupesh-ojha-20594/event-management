import { createSlice } from '@reduxjs/toolkit';

const BasicDetailsSlice = createSlice({
  name: 'BasicDetails',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default BasicDetailsSlice.reducer;
export const {} = BasicDetailsSlice.actions;
