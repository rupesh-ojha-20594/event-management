import { createSlice } from '@reduxjs/toolkit';

const SponsorExhibitorSlice = createSlice({
  name: 'SponsorExhibitor',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default SponsorExhibitorSlice.reducer;
export const {} = SponsorExhibitorSlice.actions;
