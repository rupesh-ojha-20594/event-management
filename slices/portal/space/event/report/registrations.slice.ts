import { createSlice } from '@reduxjs/toolkit';

const RegistrationsSlice = createSlice({
  name: 'Registrations',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default RegistrationsSlice.reducer;
export const {} = RegistrationsSlice.actions;
