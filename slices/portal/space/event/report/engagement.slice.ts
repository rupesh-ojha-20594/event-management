import { createSlice } from '@reduxjs/toolkit';

const EngagementSlice = createSlice({
  name: 'Engagement',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default EngagementSlice.reducer;
export const {} = EngagementSlice.actions;
