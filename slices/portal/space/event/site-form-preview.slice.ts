import { createSlice } from '@reduxjs/toolkit';

const SiteFormPreviewSlice = createSlice({
  name: 'SiteFormPreview',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default SiteFormPreviewSlice.reducer;
export const {} = SiteFormPreviewSlice.actions;
