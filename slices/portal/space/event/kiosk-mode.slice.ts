import { createSlice } from '@reduxjs/toolkit';

const KioskModeSlice = createSlice({
  name: 'KioskMode',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default KioskModeSlice.reducer;
export const {} = KioskModeSlice.actions;
