import { createSlice } from '@reduxjs/toolkit';

const NetworkRecordingSlice = createSlice({
  name: 'NetworkRecording',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default NetworkRecordingSlice.reducer;
export const {} = NetworkRecordingSlice.actions;
