import { createSlice } from '@reduxjs/toolkit';

const TaxExemptionSlice = createSlice({
  name: 'TaxExemption',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default TaxExemptionSlice.reducer;
export const {} = TaxExemptionSlice.actions;
