import { createSlice } from '@reduxjs/toolkit';

const DefaultTaxPreferenceSlice = createSlice({
  name: 'DefaultTaxPreference',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default DefaultTaxPreferenceSlice.reducer;
export const {} = DefaultTaxPreferenceSlice.actions;
