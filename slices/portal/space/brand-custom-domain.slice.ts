import { createSlice } from '@reduxjs/toolkit';

const BrandCustomDomainSlice = createSlice({
  name: 'BrandCustomDomain',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default BrandCustomDomainSlice.reducer;
export const {} = BrandCustomDomainSlice.actions;
