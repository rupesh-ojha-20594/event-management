import { createSlice } from '@reduxjs/toolkit';

const CustomizeBrandRoleSlice = createSlice({
  name: 'CustomizeBrandRole',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default CustomizeBrandRoleSlice.reducer;
export const {} = CustomizeBrandRoleSlice.actions;
