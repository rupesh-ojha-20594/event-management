import { createSlice } from '@reduxjs/toolkit';

const BrandContactGroupSlice = createSlice({
  name: 'BrandContactGroup',
  initialState: {},
  reducers: {
    // add your reducers here
  },
});

export default BrandContactGroupSlice.reducer;
export const {} = BrandContactGroupSlice.actions;
